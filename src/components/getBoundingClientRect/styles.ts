import { styled } from "@stitches/react";
export const StyledGetBoundingClientRect = styled("div", {
  position: "absolute",
  inset: "50% auto auto 50%",
  transform: "translate(-50%,-50%)",
});

export const Wrapper = styled("div", {
  position: "relative",
  width: "50%",
  height: "50%",
});
